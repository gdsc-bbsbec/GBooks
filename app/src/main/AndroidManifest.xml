<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gdsc.bbsbec.gbooks">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GBooks"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activity.ServerErrorActivity"
            android:exported="true" />
        <activity
            android:name=".activity.BookWishlistRecyclerView"
            android:exported="true" />
        <activity
            android:name=".activity.BookDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".activity.BookSearchResultRecyclerView"
            android:exported="true" />
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:exported="true" />
    </application>

</manifest>